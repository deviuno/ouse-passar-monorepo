// Re-export from local services folder
export { supabase } from './supabaseClient';
export type {
  DbUserProfile,
  DbQuestion,
  DbCourse,
  DbUserAnswer,
  DbUserReview,
  DbUserFlashcard,
  DbStudySession,
} from './supabaseClient';
